# wrk3 — text 128KB endpoint, 128 connections
[wrk3]
url = "{{ base_url }}/text/128kb"
threads = 12
connections = {{ connections }}
rate = {{ rate }}
duration_secs = {{ duration_secs }}
warmup_secs = {{ warmup_secs }}
