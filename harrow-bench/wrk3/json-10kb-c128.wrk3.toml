# wrk3 — JSON 10KB endpoint, 128 connections
[wrk3]
url = "{{ base_url }}/json/10kb"
threads = 12
connections = {{ connections }}
rate = {{ rate }}
duration_secs = {{ duration_secs }}
warmup_secs = {{ warmup_secs }}
